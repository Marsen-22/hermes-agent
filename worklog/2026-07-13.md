# 2026-07-13 — Hermes Desktop Fix + Home Canonization

**Nodes:** node1 (Mac) + node4 (Windows)

## node1 (Mac) — Desktop File Tree Fix + Dashboard Verification

**Type:** repair + verification
**Status:** complete

### Summary
The Hermes desktop app’s right-sidebar project file tree was stuck/empty. Cleared stale renderer storage, confirmed the project directory points to the canonical repo root, restarted the dashboard backend, and relaunched the app. Verified the dashboard loads in a browser tab and confirmed which dashboard plugins are functional vs. status-only.

### What Got Done

#### 1. Dashboard backend restarted
- Found `http://127.0.0.1:9119/` refusing connections.
- Started `hermes dashboard` in the background.
- Verified the dashboard returns the injected `__HERMES_SESSION_TOKEN__` and API routes are token-gated (`/api/sessions` requires `X-Hermes-Session-Token`).

#### 2. Desktop app file tree repaired
- Inspected Hermes app support directory:
  - `connection.json`: `mode: local` with empty remote block.
  - `project-dir.json`: `{"dir": "/Users/gilbertngai/Desktop/hermes/hermes-agent"}` (correct).
  - `Local Storage/leveldb/` existed with persisted renderer state.
- Killed all Hermes.app processes.
- Cleared renderer storage to force re-read of the project directory:
  ```bash
  rm -rf ~/Library/Application\ Support/Hermes/Local\ Storage
  rm -rf ~/Library/Application\ Support/Hermes/Session\ Storage
  rm -rf ~/Library/Application\ Support/Hermes/Partitions
  ```
- Relaunched `/Applications/Hermes.app`.
- Desktop app loaded with a clean session list; settings panel opened cleanly on first launch and closed; main window ready.

#### 3. Dashboard plugin reality check
- Opened `http://127.0.0.1:9119/graphify` in Chrome.
- The Graphify dashboard tab is a **status page only**:
  - Shows binary health, version, node count, edge count, last-run log tail.
  - Does **not** render the interactive graph.
- The actual graph visualization is still in `llm-wiki/graphify-out/graph.html` and must be opened/viewed separately.

### Files Touched

| Path | Action |
|------|--------|
| `~/Library/Application Support/Hermes/Local Storage` | Deleted stale renderer state |
| `~/Library/Application Support/Hermes/Session Storage` | Deleted stale renderer state |
| `~/Library/Application Support/Hermes/Partitions` | Deleted stale renderer state |
| `~/Library/Application Support/Hermes/project-dir.json` | Confirmed repo-root path |

### Verification
- `http://127.0.0.1:9119/` returns dashboard HTML with injected session token.
- Desktop app launches and shows the main window / session list.
- Graphify dashboard tile: `Status: healthy`, `Version: graphify 0.9.8`, `Nodes: 70`, `Edges: 0`.

### Notes
- Renderer storage clearing is the canonical fix for stale `ignore()` / absolute-path errors in the desktop file-tree git-ignore filter.
- Personal fork now contains all local Hermes customizations; future `hermes update` can merge from `origin` (NousResearch) while keeping personal files in fork history.

## node4 (Windows) — Hermes Home Canonization

**Type:** consolidation + canonization
**Status:** in progress

### Summary
Consolidated loose Hermes artifacts into the canonical framework home and canonized the `.hermes/` directory on node4, using node1 Mac as the primary SSOT reference.

### Completed
1. Archived 1.41 GB of corrupt/stale `state.db.*` and old config backups to `Hermes-Archive/`.
2. Archived `state-snapshots/` and moved CUDA installer outside the Obsidian vault.
3. Replaced node4 `.hermes/repo/` and `.hermes/synced/` with clean copies from node1 Mac.
4. Promoted runtime-only profiles (`analyst`, `long-context`, `scout`, `vision`) and 14 skill categories into SSOT.
5. Removed node-local `lsp/`, `mcp-installs/`, `state.db*`, caches, etc. from SSOT profiles.
6. Verified `.hermes/repo/` and `.hermes/synced/` now differ only in cluster metadata (`nodes/`, `box-identity`).

### Artifacts updated
- `worklog/2026-07-13.md`
- `FINGERPRINT.md` (consolidation + canonization log)
- `CANONIZE_PLAN.md` (full plan and decisions)

### Notes
- node1 Mac was read but not modified at user request.
- This SSOT is shared with node1; both nodes contribute read/write.

## Knowledge Base Updates
- Added `llm-wiki/session-summary-2026-07-13.md`.
- Added Desktop File Tree and Plugin Reality Check sections to `llm-wiki/hermes-web-dashboard.md`.
- Added `llm-wiki/hermes-ssot.md` documenting multi-node SSOT conventions.

## Repo Commit
- Committed all local modifications and personal workflow directories (`llm-wiki/`, `worklog/`) to personal fork `Marsen-22/hermes-agent`.
- Commit: `b7a700bcd2` — "personal: llm-wiki, worklog, local patches, and dashboard/desktop fixes"
- Follow-up: `fe9b787e65` — "docs: note fork commit in worklog and session summary"
- Push succeeded; working tree is clean.


## Gateway Cleanup (node4)

- User stopped Hermes desktop app on node4, leaving gateway open for configuration.
- Found duplicate gateway processes: one venv-spawned and one uv-spawned child.
- Identified `HermesGateway_jarvis` scheduled task using `C:\tmp\node4-gateway-wrapper.ps1` as the uv-spawn duplicate source.
- Stopped the `HermesGateway_jarvis` task and killed duplicate python processes.
- Restarted canonical `HermesGateway` scheduled task; confirmed gateway running on PID 35004 (bound to 0.0.0.0:56326 / 127.0.0.1:56325).
- `kanban.dispatch_in_gateway: true` and `gateway.multiplex_profiles: true` are already enabled on node4.
- Node4 is now ready for co-primary multi-agent configuration (Option A — independent gateways).
