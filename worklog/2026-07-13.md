# 2026-07-13 — Hermes Desktop File Tree Fix + Dashboard Verification

**Type:** repair + verification
**Status:** complete

## Summary
The Hermes desktop app’s right-sidebar project file tree was stuck/empty. Cleared stale renderer storage, confirmed the project directory points to the canonical repo root, restarted the dashboard backend, and relaunched the app. Verified the dashboard loads in a browser tab and confirmed which dashboard plugins are functional vs. status-only.

## What Got Done

### 1. Dashboard backend restarted
- Found `http://127.0.0.1:9119/` refusing connections.
- Started `hermes dashboard` in the background.
- Verified the dashboard returns the injected `__HERMES_SESSION_TOKEN__` and API routes are token-gated (`/api/sessions` requires `X-Hermes-Session-Token`).

### 2. Desktop app file tree repaired
- Inspected Hermes app support directory:
  - `connection.json`: `mode: local` with empty remote block.
  - `project-dir.json`: `{"dir": "/Users/gilbertngai/Desktop/hermes/hermes-agent"}` (correct).
  - `Local Storage/leveldb/` existed with persisted renderer state.
- Killed all Hermes.app processes.
- Cleared renderer storage to force re-read of the project directory:
  ```bash
  rm -rf ~/Library/Application\ Support/Hermes/Local\ Storage
  rm -rf ~/Library/Application\ Support/Hermes/Session\ Storage
  rm -rf ~/Library/Application\ Support/Hermes/Partitions
  ```
- Relaunched `/Applications/Hermes.app`.
- Desktop app loaded with a clean session list; settings panel opened cleanly on first launch and closed; main window ready.

### 3. Dashboard plugin reality check
- Opened `http://127.0.0.1:9119/graphify` in Chrome.
- The Graphify dashboard tab is a **status page only**:
  - Shows binary health, version, node count, edge count, last-run log tail.
  - Does **not** render the interactive graph.
- The actual graph visualization is still in `llm-wiki/graphify-out/graph.html` and must be opened/viewed separately.

## Files Touched

| Path | Action |
|------|--------|
| `~/Library/Application Support/Hermes/Local Storage` | Deleted stale renderer state |
| `~/Library/Application Support/Hermes/Session Storage` | Deleted stale renderer state |
| `~/Library/Application Support/Hermes/Partitions` | Deleted stale renderer state |
| `~/Library/Application Support/Hermes/project-dir.json` | Confirmed repo-root path |

## Verification
- `http://127.0.0.1:9119/` returns dashboard HTML with injected session token.
- Desktop app launches and shows the main window / session list.
- Graphify dashboard tile: `Status: healthy`, `Version: graphify 0.9.8`, `Nodes: 70`, `Edges: 0`.

## Remaining / Expected Behavior
- Graphify interactive visualization still requires opening `llm-wiki/graphify-out/graph.html` directly or serving it separately.
- Desktop app right sidebar should now load the `hermes-agent` repo file tree on next use.

## Notes
- Renderer storage clearing is the canonical fix for stale `ignore()` / absolute-path errors in the desktop file-tree git-ignore filter.
